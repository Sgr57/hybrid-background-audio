import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.hybrid.background_audio',
  appName: 'hybrid-background-audio',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
